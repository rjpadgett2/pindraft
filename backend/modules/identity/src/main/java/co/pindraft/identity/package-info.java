/**
 * Pindraft identity module — users, tenants, JWT, RBAC.
 */
@org.springframework.modulith.ApplicationModule(
    displayName = "Identity",
    allowedDependencies = { "common" }
)
package co.pindraft.identity;
