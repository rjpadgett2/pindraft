# domain — shared TypeScript types

Pure type definitions and constants used across apps. No code that does anything.

Use this for domain-level shapes that aren't generated by the API client (constants,
display labels, enum lists for UI dropdowns).
